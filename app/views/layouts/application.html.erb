<!DOCTYPE html>
<html>
  <head>
    <title>BlogBot</title>
    <p class="notice"><%= notice %></p>
       <p class="alert"><%= alert %></p>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application' %>
  </head>

  <body>
  	<section class="hero is-primary is-medium">
	  <!-- Hero head: will stick at the top -->
	  <div class="hero-head">
	    <nav class="navbar">
	      <div class="container">
	        <div class="navbar-brand">
	          <%= link_to 'BlogBot', root_path, class: "navbar-item" %>
	          <span class="navbar-burger burger" data-target="navbarMenuHeroA">
	            <span></span>
	            <span></span>
	            <span></span>
	          </span>
	        </div>
	        <div id="navbarMenuHeroA" class="navbar-menu">
	          <div class="navbar-end">
              <% if user_signed_in? %>
              <%= link_to "Create New Post", new_post_path, class:"navbar-item" %>

                <%= link_to "Sign out", destroy_user_session_path, :method => :delete, class:"navbar-item" %>
                <div id="currentUserSignedIn">
                  Signed in as: <%= current_user.email %>
                  <%= link_to "Edit User", edit_user_registration_path, class:"navbar-item" %>
                </div>
                <% end %>
            </div>
	        </div>
	      </div>
	    </nav>
	  </div>


	  <!-- Hero content: will be in the middle -->
	  <div class="hero-body">
	    <div class="container has-text-centered">
        <div id="slideshow">
   <div>
   <%= image_tag "robot_PNG79.png" %>
   </div>
   <div>
     <%= image_tag "omom.png" %>
   </div>
   <div>
     <%= image_tag "Robot-PNG-HD.png" %>
   </div>
</div>
	    </div>
	  </div>
	</section>
    <%= yield %>
  </body>
</html>
